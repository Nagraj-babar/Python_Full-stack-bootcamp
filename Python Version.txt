1) Write a command to get the Python version you are using?

--> There are two ways to cheak Python version which is following
   1) By using command prompt or Windows shell. when we open windows shell or cmd prompt we have to write py and then hit enter, it will show result like 
      Python 3.10.7 (tags/v3.10.7:6cc6b13, Sep  5 2022, 14:08:36) [MSC v.1933 64 bit (AMD64)] on win32
      Type "help", "copyright", "credits" or "license" for more information.

   2) In Python script we have to import sys module and then access version.
    Syntax. import sys
            print(sys.version)
      